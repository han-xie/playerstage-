#include <iostream>
#include <libplayerc++/playerc++.h>

int
main(int argc, char *argv[])
{
  using namespace PlayerCc;
int a=1;
  PlayerClient    robot("localhost",6665);
  //SimulationProxy sp(&robot,0);
  //LaserProxy lp(&robot,0);
  Position2dProxy pp(&robot,0);
  //OpaqueProxy op(&robot,0);

  char value[100];

	  //lp.RequestConfigure();
  for(;;)
  {
	  /*
	  double x,y,a;
	  //sp.GetPose2d("r0",x,y,a);
	  //std::cout<<"now pos:"<<x<<","<<y<<","<<a<<std::endl;


	  std::cout<<lp;

	  std::cout<<"continue"<<std::endl;
	  std::cin>>x;
	  */
	  //sp.SetPose2d("r0",x,x,x);
	  
	  /*
		player_opaque_data_t popa;
		int sendComd;


		popa.data_count = sizeof(sendComd);
		popa.data = (uint8_t *) &sendComd;
		op.SendCmd(&popa);
	*/

	  double x,y,a;
	  std::cout<<"x"<<std::endl;
	  std::cin>>x;
	  std::cout<<"y"<<std::endl;
	  std::cin>>y;
	  std::cout<<"a"<<std::endl;
	  std::cin>>a;
	  pp.SetSpeed(x,y,a);
  }
}


